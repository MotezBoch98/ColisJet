import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'Customers | ColisJet Dashboard', // Or your preferred title
};

export default function Page() {
  return <p>Customers Page</p>;
}
